% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{cluster_sce}
\alias{cluster_sce}
\alias{cluster_seurat}
\title{Cluster cells in a SingleCellExperiment}
\usage{
cluster_sce(
  sce,
  assay_name,
  do.scale = FALSE,
  do.center = FALSE,
  resolution = 0.8,
  n.neighbors = 10,
  npcs.pca = 50,
  features.pca = "all",
  nvar.features = NULL,
  dims = NULL,
  k.param = 20,
  suffix = "",
  PCA_name = paste0("PCA", suffix),
  UMAP_name = paste0("UMAP", suffix),
  cluster_name = paste0("clusters", suffix),
  umap.metric = "cosine",
  run_umap = TRUE,
  verbose = TRUE
)

cluster_seurat(sce, assay_name, ...)
}
\arguments{
\item{sce}{SingleCellExperiment object}

\item{assay_name}{Assay name. Provide two names to jointly cluster across both.}

\item{do.scale, do.center}{Scale/center features before PCA}

\item{resolution}{Leiden resolution parameter}

\item{n.neighbors}{Neighbours for UMAP}

\item{npcs.pca}{Number of principal components to compute}

\item{features.pca}{One of \code{'all'}, \code{'variable'}, or a vector of features}

\item{nvar.features}{Number of variable features when \code{features.pca = 'variable'}}

\item{dims}{PCs to use for the graph and UMAP (default: all computed)}

\item{k.param}{k for the nearest-neighbour graph}

\item{suffix}{Suffix appended to the output PCA/UMAP/cluster names}

\item{PCA_name, UMAP_name, cluster_name}{Output names}

\item{umap.metric}{UMAP distance metric}

\item{run_umap}{Logical; compute a UMAP embedding (requires \code{uwot})}

\item{verbose}{Message verbosity}

\item{...}{(For the deprecated \code{cluster_seurat()} alias) further arguments
passed to \code{cluster_sce()}.}
}
\value{
SingleCellExperiment with PCA/UMAP in \code{reducedDims} and cluster
assignments in \code{colData}.
}
\description{
SCE-native clustering workflow: PCA (\code{\link[irlba:prcomp_irlba]{irlba::prcomp_irlba()}}) ->
shared-nearest-neighbour graph (\code{\link[BiocNeighbors:findKNN-methods]{BiocNeighbors::findKNN()}} + Jaccard
weighting) -> Leiden community detection (\code{\link[igraph:cluster_leiden]{igraph::cluster_leiden()}}) ->
UMAP embedding (\code{\link[uwot:umap]{uwot::umap()}}). This replaces the former Seurat-based
implementation and carries no Seurat dependency.

\code{cluster_seurat()} is a deprecated alias for \code{cluster_sce()}.
}
